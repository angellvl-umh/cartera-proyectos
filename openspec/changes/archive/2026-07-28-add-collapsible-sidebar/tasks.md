## 1. Frontend (usar opencode, no kiro-cli)

- [x] 1.1 En `app.component.ts`, importar `BreakpointObserver`/`Breakpoints` de `@angular/cdk/layout` (`provideHttpClient`-style: inyectar con `inject(BreakpointObserver)`) y suscribirse a `observe('(max-width: 992px)')` para forzar `collapsed.set(true)`/`collapsed.set(false)` al cruzar el breakpoint (usar `toSignal` o una suscripción gestionada con `takeUntilDestroyed`, siguiendo el estilo ya usado en el fichero con `toSignal` para otros observables)
- [x] 1.2 Añadir un botón en la cabecera (`<header>`, junto al breadcrumb o al bloque de usuario) con icono `menu-fold`/`menu-unfold` (ya registrados en `app.config.ts`) que alterna `collapsed` al pulsarlo
- [x] 1.3 Ligar el ancho del `<aside>` a `collapsed()`: `252px` expandido, `72px` plegado (usar `[style.width]` en vez del `width:252px` fijo actual), con una transición CSS suave (`transition: width 150ms ease` o similar)
- [x] 1.4 Ocultar las etiquetas de texto de cada `sidebar-nav-item` cuando `collapsed()` es true (envolver los `<span>` de texto, no los `<span nz-icon>`, en `@if (!collapsed())`), y añadir `nz-tooltip` con el texto del enlace a cada `<a class="sidebar-nav-item">` para que siga siendo identificable en modo icono (importar `NzToolTipModule`)
- [x] 1.5 Ocultar también los encabezados de sección ("Principal", "Administración") cuando `collapsed()` es true, ya que no tienen sentido sin las etiquetas de los ítems debajo
- [x] 1.6 Persistir `collapsed` en `localStorage` (clave `sidebar-collapsed`): leer el valor guardado al inicializar el componente (si no hay valor guardado, usar el resultado del breakpoint observer en el arranque) y escribirlo cada vez que `collapsed` cambie (por toggle manual o por breakpoint)
- [x] 1.7 Ajustar el logo/cabecera del `<aside>` para que quede centrado y legible en modo plegado (el bloque "UMH" de 38x38 ya no depende de `collapsed()` para su visibilidad, solo el subtítulo de texto — revisar que no quede descentrado a 72px de ancho)

## 2. Verificación

- [x] 2.1 `ng build` sin errores de TypeScript
- [x] 2.2 Verificado manualmente con Playwright contra el stack Docker reconstruido (login real como `gestor`): toggle manual (✓), auto-colapso al cruzar 992px hacia abajo (✓), auto-expansión al cruzar hacia arriba (✓), el toggle manual no es sobrescrito por el auto-colapso mientras la ventana sigue estrecha (✓), persistencia en `localStorage` tras recargar (✓). El tooltip en modo plegado no se pudo confirmar visualmente vía Playwright — se comprobó que el mismo problema (`.cdk-overlay-container`/`.ant-tooltip` nunca aparece tras `hover()`) reproduce también en un `nz-tooltip` preexistente y no tocado por este change (botón "Editar" en `persons-list.component.ts`), así que es una limitación del entorno de test (o del build de Playwright/CDK), no una regresión de esta feature — el código sigue el mismo patrón `nz-tooltip`/`nzTooltipTitle` ya usado con éxito en producción en el resto de la app.
