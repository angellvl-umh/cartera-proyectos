## 1. Backend

- [x] 1.1 Crear `Core/Features/Activity/GetActivityFeed.cs`: `GetActivityFeedQuery(int? ProjectId, int? TeamId, int? PersonId, int Page = 1, int PageSize = 20) : IRequest<PagedResult<ActivityEventDto>>` y `ActivityEventDto(string Type, DateTimeOffset OccurredAt, int ProjectId, string ProjectTitle, int ActorId, string ActorName, string Summary)`. `Type` es uno de `"ProjectStatusChanged"`, `"WorkItemCreated"`, `"WorkItemCompleted"`, `"CommentAdded"`, `"WeeklyUpdateRegistered"`.
- [x] 1.2 Implementar el handler siguiendo design.md - Decisiones ("5 queries EF filtradas + merge en memoria"): por cada uno de los 5 tipos, una query EF que aplica los filtros (`ProjectId` directo o vía `WorkItem.ProjectId` para comentarios; `TeamId` vía `db.ProjectTeamAssignments.Where(a => a.TeamId == teamId).Select(a => a.ProjectId)`; `PersonId` vía `ChangedById`/`AuthorId`), ordena por su columna de fecha descendente y aplica `Take(page * pageSize)`; excluir en `ProjectStatusHistory` las entradas con `FromStatus == null` (creación, no "cambio"). Normalizar cada fecha (`DateTime`, UTC por convención) a `DateTimeOffset` con offset cero al proyectar a `ActivityEventDto`.
- [x] 1.3 Formatear `Summary` por tipo dentro del handler: cambio de estado → `"De {FromStatus} a {ToStatus}"`; tarea creada/completada → el `Title` de la tarea; comentario → `Text` truncado a 140 caracteres (`+ "…"` si se trunca); avance semanal → el `Summary` del `ProjectWeeklyUpdate`.
- [x] 1.4 Combinar los 5 resultados ya filtrados en memoria, ordenar por `OccurredAt` descendente, aplicar `Skip((page-1)*pageSize).Take(pageSize)` para la página pedida (con `pageSize` acotado a 100 y `page` mínimo 1, mismo patrón que `GetOrganicUnitsHandler`). Calcular `Total` con 5 `CountAsync` (mismos filtros) sumados, sin traer filas de más.
- [x] 1.5 Añadir endpoint `GET /api/activity` en `Api/Endpoints/ReportEndpoints.cs` (parámetros de query `projectId`, `teamId`, `personId`, `page`, `pageSize`), `RequireAuthorization()`, `.WithName("GetActivityFeed")`, descripción OpenAPI en español — mismo patrón que los demás endpoints de ese fichero (p. ej. `/api/reports/weekly-portfolio`).

## 2. Frontend

- [x] 2.1 Crear `src/frontend/src/app/features/reports/activity-feed.component.ts` (standalone, `OnPush`, signals): filtros de proyecto/equipo/persona (selects poblados vía `HttpClient` a `/api/projects`, `/api/teams`, `/api/persons`, mismo patrón `toSignal(this.http.get(...))` que `weekly-portfolio-report.component.ts`), lista de eventos con icono y color por `Type` (reutilizar la paleta de `NzTag`/iconos ya usada en el resto de informes), y paginación (`nz-pagination` o equivalente ya usado en otro listado paginado del proyecto).
- [x] 2.2 Registrar la ruta `reports/activity` en `src/frontend/src/app/app.routes.ts` (`loadComponent` del nuevo componente), junto a la entrada existente de `reports/weekly-portfolio`.
- [x] 2.3 Añadir el enlace de navegación en `src/frontend/src/app/app.component.ts`: nueva entrada en `ROUTE_LABELS` (p. ej. `'reports-activity': 'Feed de actividad'`) y un `<a routerLink="/reports/activity">` junto al de "Informe semanal" (mismo estilo `padding-left:34px`, submenu de Cartera). **No reutilizar la etiqueta "Actividad"** — ya está tomada por `/team-activity` ("Trabajo en curso"), que es una vista distinta (tareas activas por persona, no un histórico); usar "Feed de actividad" para evitar confusión.

## 3. Tests

- [x] 3.1 Tests unitarios de `GetActivityFeedHandler` en `tests/CarteraProyectos.UnitTests/Features/Activity/`: feed con los 5 tipos de evento mezclados y ordenados cronológicamente; la entrada de creación de `ProjectStatusHistory` (`FromStatus == null`) no aparece; filtro por `ProjectId` (incluye comentarios de tareas de ese proyecto); filtro por `TeamId`; filtro por `PersonId`; filtros combinados; paginación (página 2, `pageSize` por encima del máximo se recorta, `Total` refleja los filtros aplicados).
- [x] 3.2 Test unitario o de integración ligero del endpoint `GET /api/activity`: 401 sin autenticación.
- [x] 3.3 `dotnet build src/CarteraProyectos.slnx` y `dotnet test src/CarteraProyectos.slnx` en verde.
- [x] 3.4 `pnpm build` (`ng build`) del frontend en verde.
