## 1. Frontend — `chat-panel.component.ts`

- [ ] 1.1 Quitar el guard `@if (activeConvId() === null) { <nz-empty "Selecciona o crea una conversación"> } @else { <mensajes + input-bar> }`: el `.input-bar` pasa a renderizarse siempre. El área de mensajes muestra el `nz-empty` "Sin mensajes todavía" (el que ya existe para `visibleMessages().length === 0`) tanto si no hay conversación activa como si la hay pero está vacía; en cualquier otro caso, la lista de mensajes normal.
- [ ] 1.2 Extraer el cuerpo actual de `sendMessage()` (a partir del mensaje optimista, línea ~455 en adelante) a un método privado `continueSend(convId: number, text: string): void` con el mismo comportamiento de hoy (mensaje optimista, `chatService.sendMessage`, recarga de mensajes, manejo de `hitIterationLimit` y de error).
- [ ] 1.3 Reescribir `sendMessage()`: valida `text` no vacío y `!sending()` igual que hoy; si `activeConvId() === null`, pone `sending.set(true)`, deriva el título con `deriveTitle(text)` (nuevo método privado: `text.split('\n')[0].trim()`, recortado a 60 caracteres con `…` si excede), llama a `chatService.createConversation(título)`, y en el `next` hace `activeConvId.set(id)` + `loadConversations()` + `continueSend(id, text)`; en el `error` hace `sending.set(false)` y `message.error('No se pudo crear la conversación')` sin tocar `inputText` (el texto se conserva para reintentar). Si `activeConvId()` ya tiene valor, llama directamente a `continueSend(convId, text)` (comportamiento actual, sin cambios).
- [ ] 1.4 En `continueSend`, antes de aplicar los efectos del `next`/`error` de `chatService.sendMessage(convId, text)` (actualizar `rawMessages`, `sending`, la entrada de `conversations`, mostrar el warning de `hitIterationLimit`, restaurar `inputText` en error), comprobar `this.activeConvId() === convId`. Si ya no coincide (el usuario cambió de conversación o abrió un borrador mientras la petición estaba en curso), sigue actualizando `conversations` (contador de mensajes / `updatedAt`) para que el historial quede correcto, pero no toques `rawMessages`, `sending`, `inputText` ni el mensaje optimista de la vista actual — pertenece a otra conversación.
- [ ] 1.5 Reescribir `startNewConversation()`: sin llamada a `chatService.createConversation` ni a `listConversations()`. Nuevo cuerpo: `this.activeConvId.set(null); this.rawMessages.set([]); this.inputText = ''; this.sending.set(false);`.
- [ ] 1.6 Revisar que `selectConversation(id)` siga funcionando igual (no requiere cambios funcionales), pero añade `this.sending.set(false)` al principio del método — si había un envío en curso para la conversación/borrador anterior, la vista nueva no debe arrancar deshabilitada por ese estado ajeno (el guard de la tarea 1.4 ya evita que la respuesta tardía la reactive incorrectamente).
- [ ] 1.7 Revisar `deleteConversation(id)`: si `id` es la conversación activa, ya deja `activeConvId`/`rawMessages` a blanco (comportamiento actual) — añade también `this.inputText = ''` por coherencia con el nuevo estado de "borrador limpio" tras 1.5.

## 2. Tests (Vitest)

- [ ] 2.1 En `chat-panel.component.spec.ts` (o un fichero nuevo `chat-panel.component.conversation-flow.spec.ts` si se prefiere no mezclar con los tests existentes de markdown), añadir tests para `deriveTitle`: primera línea de un texto multilínea, recorte a 60 caracteres con `…` en textos largos, texto corto se devuelve tal cual.
- [ ] 2.2 Test de `sendMessage()` sin conversación activa: mockea `ChatService.createConversation` y `ChatService.sendMessage`; comprueba que se llama primero a `createConversation` con el título derivado del texto, que `activeConvId` queda al id devuelto, y que después se llama a `sendMessage` con ese id y el texto original.
- [ ] 2.3 Test de `sendMessage()` cuando `createConversation` falla: comprueba que `sending()` vuelve a `false`, que se muestra el error, y que `inputText` conserva el texto escrito (no se pierde).
- [ ] 2.4 Test de la guarda de conversación activa: simula un envío en curso (`continueSend`) y, antes de que resuelva el observable, cambia `activeConvId` a otro valor (simulando `selectConversation`/`startNewConversation`); comprueba que al resolver la respuesta tardía, `rawMessages` de la vista actual no se sobrescribe.
- [ ] 2.5 Test de `startNewConversation()`: comprueba que NO se invoca `chatService.createConversation`, y que `activeConvId`, `rawMessages` e `inputText` quedan reseteados.
- [ ] 2.6 `pnpm test -- --run` en verde (todos los tests del proyecto, incluidos los de markdown ya existentes, sin regresiones).

## 3. Validación

- [ ] 3.1 `dotnet build src/CarteraProyectos.slnx` y `dotnet test src/CarteraProyectos.slnx` en verde (sin cambios esperados, pero se verifica que nada se ha roto — este change no toca backend).
- [ ] 3.2 `pnpm build` (`ng build`) del frontend en verde, sin nuevos warnings de `tsc`/plantilla por el guard eliminado.
